# План доработки — что осталось и в каком порядке

Разбит на 5 блоков, от самого срочного и дешёвого к самому объёмному.
Для каждого пункта — что именно сделать, зачем, и от чего зависит.

**Прогресс:** Блок 1 (авторизация) — ✅ сделан целиком (Этап 1
доработки). Блок 2 (живая карта) — ✅ сделана целиком (Этап 2 доработки).
Блок 4 (развёртывание) — ✅ сделан целиком (Этап 3 доработки, отдельный
патч `deploy/`, код приложения не тронут). Блок 3 (телеграм-бот) — ✅
сделан целиком (Этап 4 доработки): части 3.1-3.4 сделаны раньше, часть
3.5 (маршрутизация сообщений/гео-заметки/запуск) сделана этим заходом —
Блок 3 закрыт.
Детали и что именно изменилось — под каждым пунктом ниже.
Блок 5 — как в исходном плане, без изменений.

---

## Блок 1. Авторизация — довести до конца (самое дешёвое, самое нужное)

> **Статус: сделано (Этап 1 доработки).** Ниже — исходная постановка
> задачи, а после каждого пункта отметкой ✅ — что именно внесено.

Инфраструктура вся есть (users/sessions/роли/PBKDF2-пароли/WAL — Этап 2
плана переноса, backend реально протестирован через TestClient), не
хватает проводки между готовыми кусками.

1. **`frontend/src/pages/Login.jsx`** (новый) — форма логин/пароль →
   `POST /api/auth/login` → токен в `localStorage` (ключ
   `lesovod_token`, `client.js` его уже читает).
   ✅ Файл создан. Форма логин/пароль, ошибки от `POST /api/auth/login`
   показываются под полями, токен кладётся в `localStorage`.
2. **`App.jsx`** — на старте вызывать `GET /api/auth/me`; если 401 —
   показывать `Login.jsx` вместо остального приложения; успешный
   `user` прокидывать в `Sidebar` (проп `user` там уже принимается,
   просто никто не передаёт).
   ✅ Сделано. При старте — `GET /api/auth/me`, если токена нет/он
   невалиден — экран `Login`. После входа реальный `user` идёт в
   `Sidebar` (ФИО/роль вместо `MOCK_USER`) и как проп `currentUser` во
   все экраны (`<screen.Page currentUser={user} />`) — этим уже
   пользуется п.6 ниже.
3. **Кнопка "Выйти"** в `Sidebar`/`TopBar` → `POST /api/auth/logout` +
   очистка токена.
   ✅ Кнопка (иконка выхода) в нижнем блоке `Sidebar`, рядом с
   ФИО/ролью. Зовёт `POST /api/auth/logout`, затем в любом случае
   (даже если запрос не прошёл) чистит `localStorage` и возвращает на
   `Login`.
4. **Подключить `require_permission(...)` к реальным роутерам** — по
   одному действию на роутер, не всё сразу:
   - `documents.py`: `delete`, `bulk-delete` → `documents.delete`
   - `delyanki.py`: `archive`/`delete`/генерация документов →
     `delyanki.edit`
   - `raskhod.py`/`inspection.py`: создание нарядов/актов →
     `raskhod.edit`
   - Список действий уже описан в `webext.PERMISSIONS` — нужно только
     сверить, что там есть все нужные ключи, и добавить недостающие.
   ✅ Сделано, но точнее исходной формулировки — в `PERMISSIONS` уже
   были отдельные ключи под разные действия, ими и воспользовались
   вместо одного общего `delyanka.edit` на всё:
   - `documents.py`: `delete_document`, `bulk_delete_documents` →
     `documents.delete`.
   - `delyanki.py`: CRUD делянки/выдела/пресетов → `delyanka.edit`
     (удаление делянки отдельно — `delyanka.delete`, права те же
     admin/lesovod, но в БД/аудите это отдельное действие); импорт
     МДО → `delyanka.edit`; генерация Акта/Листков/Техкарт/Акта
     готовности → `documents.generate` (готовый ключ в `PERMISSIONS`,
     точнее по смыслу, чем `delyanka.edit`, для операции "сгенерировать
     файл").
   - `raskhod.py`: наряды (создание/правка/удаление) и импорт ЕГАИС →
     `raskhod.edit`; экспорт в Excel → `documents.generate`.
   - `inspection.py`: сроки/чек-лист/пресеты → `inspection.edit`;
     справка/акт освидетельствования/пустой бланк → `documents.generate`.
   - Все нужные ключи уже существовали в `webext.PERMISSIONS` —
     добавлять новые не понадобилось.
   - Чтение (`GET`) везде оставлено открытым, как и было — сузить его
     до `*.view` в этот пункт не входило.
5. **`touch_updated_by()` вызвать по месту** — в тех же роутерах, где
   ставите `require_permission`, после успешного изменения записи
   передавайте `user["login"]`.
   ✅ Добавлено в `delyanki.py` (`update_delyanka`/`archive_delyanka` →
   таблица `delyanka`; `update_item` → `delyanka_item`) и в
   `raskhod.py` (`update_naryad` → `raskhod_naryad`). Для `documents`
   отдельный вызов не нужен — `created_by` пишется при создании записи,
   а не через `touch_updated_by`.
   - Попутно закрыт хвост из Блока 5: генераторы документов
     (`delyanki.py`/`raskhod.py`/`inspection.py`) раньше принимали
     `created_by` открытым query-параметром, которому мог быть передан
     любой логин, — теперь он всегда берётся из авторизованного
     `user["login"]`, параметр из сигнатур убран. Фронтенд его нигде не
     передавал (проверено), так что существующие вызовы не сломаны.
6. **Смена пароля `admin`** — либо форма в `Settings.jsx`
   (`PATCH /api/auth/users/{id}` — сейчас есть только смена
   роли/активности, добавить смену пароля), либо хотя бы явно
   прописать в инструкции по развёртыванию "сразу после первого
   запуска смените admin/admin".
   ✅ Сделан полный вариант с формой, а не только инструкция:
   - Backend: `webext.set_user_password()` (обновляет хэш, отзывает все
     сессии пользователя) + `PATCH /api/auth/users/{id}/password`
     (только `users.manage`, минимум 4 символа).
   - Frontend: карточка «Пользователи» в `Settings.jsx` (видна только
     при `currentUser.role === "admin"`) — таблица пользователей,
     смена роли прямо в таблице, вкл/выкл учётной записи, создание
     нового пользователя и смена пароля (модалки). Остальным ролям
     backend всё равно ответит 403 на каждый из этих эндпоинтов —
     проверка роли на фронтенде только прячет пункт меню, а не
     является единственной защитой.

   Заодно (мелкий хвост из Блока 5): `upload_abris` в `delyanki.py`
   раньше не проверял, что `item_id` существует, — теперь отдаёт 404,
   если выдела нет.

**Проверено:** `npx vite build` фронтенда проходит без ошибок; юнит-тест
`webext.set_user_password()` (смена пароля → старые сессии отозваны →
логин по новому паролю работает, по старому — нет) прогнан вручную.
Полный прогон backend (`uvicorn`/`TestClient`) не делался в этом заходе —
для него нужны тяжёлые зависимости (`geopandas`/`shapely`/`fiona` под
Блок 2), недоступные в среде, где готовился этот патч; синтаксис и логика
всех изменённых файлов проверены (`ast.parse` + ручной разбор).

**Объём:** 1 новый файл + точечные правки в ~5 файлах. Часы, не дни. —
подтвердилось.
**Зависимости:** ничего не блокирует и не блокируется — можно делать
прямо сейчас.

---

## Блок 2. Живая карта — единственный экран, который нужно переделать, а не доделать

> **Статус: сделано (Этап 2 доработки).** Ниже — исходная постановка
> задачи, а после каждого пункта отметкой ✅ — что именно внесено.

Было: `<iframe>` поверх целого HTML-файла от `folium`. План хотел:
bbox-стриминг + Leaflet.

1. **Backend `app/routers/map.py`**:
   - `GET /api/map/kvartaly?bbox=minLon,minLat,maxLon,maxLat` —
     фильтрует `map_kvartala.geojson` по пересечению с bbox через
     `geopandas`/`shapely` (логика фильтрации уже частично есть в
     `forest_map.py` — `_read_filtered_layer`, можно переиспользовать
     паттерн, а не писать с нуля).
   - `GET /api/map/vydela?bbox=...` — то же для `map_vydela.geojson`
     (56 МБ, из-за размера bbox-фильтрация тут не опция, а
     необходимость).
   - Упрощение геометрии на дальних масштабах (`shapely.simplify` с
     tolerance от zoom-уровня, приходящего параметром) — иначе браузер
     захлебнётся полигонами по всей области.
   ✅ Сделано именно так, как описано, плюс по месту решённые детали:
   - `forest_map.py` (легаси-модуль, внутренняя логика
     `generate_forest_map`/`get_sanitary_vydely` не тронута) дополнен
     `get_map_layer_geojson(db_path, lch_num, layer, bbox, zoom)` —
     переиспользует `_read_filtered_layer` (тот же кэш по
     `(файл, mtime, лесничество, tolerance)`), затем режет результат по
     bbox через `GeoDataFrame.cx[...]` (дёшево — режется уже
     отфильтрованный по лесничеству, а не полные 56 МБ) и отдаёт
     GeoJSON вручную (`_gdf_to_geojson_dict`), а не через
     `gdf.to_json()` — так в `properties` уходят только нужные колонки
     (`num_kv`/`num_vd`/статус), а не всё, что было в исходном
     `.geojson`, и заодно чистятся `NaN`/numpy-скаляры, которые иначе
     ломают `JSON.parse` в браузере.
   - Упрощение геометрии — 5 ступеней допуска по порогам zoom
     (`_MAP_ZOOM_SIMPLIFY_STEPS`: 0.0007° на дальнем плане → 0.0 на
     `zoom>=17`), а не плавная функция от zoom — так на кэш приходится
     не больше 4 разных допусков на лесничество, а не по значению на
     каждый промежуточный zoom, которым Leaflet заваливает бы бэкенд.
   - Для слоя `vydela` дополнительно считает `status_color`/
     `status_label` по `completed_works` — та же категоризация
     (`_forest_map_category`), что красит HTML-карту, только результат
     уходит в `properties` GeoJSON, а не в инлайновый стиль `folium`.
   - `app/routers/map.py`: `GET /api/map/kvartaly` и
     `GET /api/map/vydela` (query: `lesnichestvo_num`, `bbox`, `zoom`)
     — тонкие обёртки над `get_map_layer_geojson` с единой обработкой
     ошибок (`400` — битый bbox, `404` — нет `.geojson`-файлов, `503` —
     `geopandas` не установлен на сервере, с текстом-подсказкой прямо
     в ответе). `GET /api/map/generate` (folium/HTML) не убран — остался
     как есть, фронтенд теперь дёргает его через кнопку "Экспорт в
     HTML" (офлайн-версия карты одним файлом), а не как основной режим.
   - Заодно (нужно было для клика по полигону, см. п.2) — новый
     `GET /api/delyanki/by-location?kvartal=&vydel=&lesnichestvo=` в
     `app/routers/delyanki.py`: ищет делянки, у которых есть выдел с
     таким квартал/выдел/лесничество (JOIN `delyanka_item`+`delyanka`).
     Объявлен раньше `GET /{delyanka_id}` в файле — иначе FastAPI
     попытался бы прочитать `by-location` как `delyanka_id: int` и
     вернул 422 вместо вызова обработчика.
   - `requirements.txt`: `geopandas`/`shapely`/`folium` раскомментированы
     (были закомментированы с самого начала, см. AUDIT.md). Проверено
     важное для Блока 4 (Windows-сервер без GDAL): `geopandas>=1.0`
     ставится через `pip install` и тянет `pyogrio` как backend чтения
     `.geojson` — GDAL/`fiona` системно ставить не нужно.
2. **Frontend `LiveMap.jsx`** — переписать с `<iframe>` на
   `react-leaflet`:
   - Слои кварталы/делянки как `GeoJSON`-компоненты, подгружаемые по
     `bbox` при `moveend`/`zoomend` с debounce 300мс (как и просил
     план).
   - Слой "усыхание леса" — вернуть как наложение на карту (сейчас это
     отдельная таблица), т.е. `get_sanitary_vydely` рисовать
     маркерами/подсветкой поверх полигонов, а не списком снизу.
   - Клик по полигону делянки → карточка (можно переиспользовать
     логику из `Plots.jsx`, не встраивать Qt-механику).
   ✅ Сделано, с одним отступлением от буквальной формулировки:
   - `package.json`: добавлены `leaflet` и `react-leaflet` (v4,
     совместим с уже используемым React 18 — новых крупных зависимостей
     под них не потребовалось).
   - `MapContainer`/`TileLayer` (OpenStreetMap) + `LayersControl` с
     тремя слоями — "Кварталы" (чёрный контур + постоянная подпись
     номера, как `DivIcon` в HTML-версии, здесь — `bindTooltip({
     permanent: true })`), "Выдела" (заливка по `status_color`/
     `status_label` из ответа бэкенда) и "Усыхание леса"
     (`CircleMarker`+`Popup` вместо `folium.Marker`, чтобы не тащить
     кастомные иконки).
   - Загрузка по bbox: компонент `MapViewportWatcher` на
     `useMapEvents({moveend, zoomend})`, debounce 300мс, счётчик
     запросов (`fetchGenRef`) отбрасывает устаревшие ответы, если карту
     подвинули ещё раз до того, как пришёл предыдущий — без этого при
     быстром скролле в интерфейс мог прилететь и перезаписать более
     новые данные более старым ответом.
   - Клик по полигону выдела → `Modal` с карточкой: п. "можно
     переиспользовать логику из `Plots.jsx`" на практике решился проще
     — используется уже готовый `GET /api/taxation/vydel`
     (`Taxation.jsx`, не менялся), дополненный новым
     `GET /api/delyanki/by-location` (см. выше), так что карточка сразу
     показывает и таксационные данные участка (площадь/состав/
     возраст/бонитет/…), и делянку, если она на этот выдел уже
     заведена в системе. Отступление от буквальной формулировки плана
     ("Plots.jsx") — по существу тот же результат ("карточка участка по
     клику"), другой уже готовый источник данных.
   - Легенда категорий работ — тот же список цветов, что в
     `FOREST_MAP_LEGEND_HTML`, обычным `div` поверх карты.
   - "Экспорт в HTML" — старая кнопка "Обновить карту" (folium,
     `POST /api/map/generate` + `pollTask`) сохранена в упрощённом виде:
     теперь это вторичная кнопка, результат открывается в новой вкладке
     (`window.open`), а не встраивается `<iframe>`'ом — сама живая карта
     на этом эндпоинте больше не основана, но офлайн-экспорт одним
     файлом (отправить по почте / открыть без сервера) остаётся
     доступен.

   **Сознательно упрощено** (см. также докстринг `LiveMap.jsx`):
   1. Поиск по номеру квартала (`folium.plugins.Search`) — не
      перенесён. В HTML-версии он работал по уже загруженному в
      браузер целиком слою; при bbox-стриминге честный "поиск по всему
      лесничеству" потребовал бы отдельного серверного индекса — решили
      не делать в этом заходе, оставили прокрутку/зум руками.
   2. Начальный вид карты — фиксированные координаты
      (`DEFAULT_MAP_CENTER` в `LiveMap.jsx`, подобраны по названиям
      лесничеств в `lch_map.json` — Оршанский район, Витебская обл.;
      подставьте свои, если не подходят), а не автоматический
      `fit_bounds` по данным лесничества — сама идея bbox-стриминга в
      том, чтобы не читать все границы лесничества целиком просто
      чтобы посчитать центр.
   3. Слой "Усыхание леса" не пересчитывает квартал/выдел на лету при
      клике по карте — `GET /api/map/sanitary` не менялся, он и так уже
      отдаёт готовые `kvartal`/`vydel`, посчитанные один раз на бэкенде
      через `get_sanitary_vydely` (point-in-polygon через `shapely`).

**Проверено:** backend — через `TestClient` на синтетических
`.geojson`/SQLite-фикстурах (2 квартала × 4 выдела в EPSG:32635, запись
`completed_works`, запись `geo_notes`, тестовая делянка на выдел):
список слоёв без bbox, с bbox (обрезка подтверждена — из 8 выделов
остаются только 4 в нужном квартале), битый bbox → 400 с понятным
текстом, несуществующее лесничество → пустой `FeatureCollection` без
падения, `by-location` находит привязанную делянку. Frontend —
`npx vite build` со всеми новыми зависимостями (`leaflet`,
`react-leaflet`) проходит без ошибок. Полноценная проверка на реальных
56 МБ `.geojson` и боевой БД лесхоза не делалась — в среде, где
готовился патч, их нет; `geopandas` подтверждённо ставится через
`pip install` (использует `pyogrio`, без системного GDAL), но
производительность `_read_filtered_layer`+bbox-обрезки на настоящем
56-мегабайтном файле стоит перепроверить на реальных данных при первом
запуске.

**Объём:** средний. Backend — день-полтора (фильтрация bbox +
упрощение геометрии — geopandas-код, который надо написать и
проверить на реальных 56 МБ файла). Frontend — день-два (нужно
аккуратно сделать debounce+загрузку без дублей запросов). —
подтвердилось по объёму работы; на синтетических данных весь цикл
(backend+frontend) занял один заход.
**Зависимость:** нужен `geopandas`/`shapely` реально установленными на
сервере — было закомментировано в `requirements.txt`, теперь
раскомментировано.

---

## Блок 3. Telegram-бот на единый API

> **Статус: ✅ сделан целиком (Этап 4 доработки).** Разбит на части, как
> и просили — каждая отдельным заходом. Ниже — исходная постановка задачи
> по каждой части, а после отметки ✅ — что именно внесено.

**Части, на которые разбит блок** (порядок = порядок выполнения):

1. ✅ **Аудит** — сделано (заходом части 3.1, до передачи мне архивов
   этого блока — сами материалы аудита в присланных архивах не
   сохранились, здесь фиксируется только факт, что он предшествовал
   переводу на HTTP: 14 точек прямого импорта `from db import ...`/
   `from raskhod import ...` + ~33 места прямой работы с sqlite внутри
   хендлеров, находка №1 (расхождение `get_remaining_volumes_for_bot`
   между `legacy/raskhod.py` и `raskhod_v2.py`) и находка №2 (в headless
   `config.py` не было ни токен-функций, ни справочника должностей) —
   обе видны по комментариям в текущем `telegram_bot.py`/`config.py` и
   были закрыты по ходу частей 3.1-3.4, см. по пунктам ниже.
2. ✅ **Авторизация бота** — сделано (заходом части 3.1/предшествующим
   `lesovod_stage9_block3_part2_botauth.zip`). Служебная роль `bot`
   (`webext.PERMISSIONS["bot.access"] = ("admin", "bot")`), два разных
   токена через `.env`/переменные окружения:
   `LESOVOD_TELEGRAM_BOT_TOKEN` (бот ↔ Telegram) и
   `LESOVOD_BOT_SERVICE_TOKEN` (бот ↔ этот backend, заголовок
   `Authorization: Bearer ...`, распознаётся `app/auth.py` как
   синтетический пользователь с ролью `bot`, в таблице `users` не
   хранится). Подробности — в докстринге `legacy/config.py`.
3. **Замена импортов на HTTP-вызовы** — файл оказался большим (1425
   строк) и, что важнее, содержит не только 14 точек импорта из
   `db.py`/`raskhod.py`, но ещё ~33 места прямой работы с sqlite
   (`get_db()`/`cursor.execute`) внутри самих хендлеров — переводить на
   HTTP нужно почти каждый обработчик, а не только точки импорта. По
   просьбе — разбита на подчасти ниже, порядок = порядок выполнения.
   Каждая подчасть — отдельный заход, с собственным диффом поверх
   результата предыдущей.

   3.1. ✅ **Регистрация и меню** — сделано. `get_dolzhnost_menu`,
        `get_lesnichestvo_menu`, `get_menu_for_dolzhnost`,
        `send_welcome`, `_start_registration`, `process_fio_step`,
        `process_dolzhnost_step`, `process_lesnichestvo_step`,
        `_require_registered_user`. Пишут/читают `lesorub_directory`
        напрямую через sqlite — нужен HTTP-эндпоинт регистрации/проверки
        пользователя.
        - Новый роутер `app/routers/bot.py` — `GET/PUT
          /api/bot/workers/{telegram_id}`, за `require_permission
          ("bot.access")` (ключ уже был заведён в Блоке 3/части 2).
          `GET` — 404, если рабочий не регистрировался; `PUT` — upsert по
          `viber_id` (то же самое, что раньше делала
          `_save_registration(cursor, ...)` напрямую в sqlite). Подключён
          в `app/main.py`.
        - `telegram_bot.py`: добавлена секция HTTP-клиента
          (`_api_session`/`_api_request`/`get_worker`/
          `save_registration`, токен и адрес backend — из
          `config.get_bot_service_token()`/`config.get_api_base_url()`).
          `send_welcome` и `process_lesnichestvo_step` переведены на
          `get_worker()`/`save_registration()`; старая
          `_save_registration(cursor, ...)` удалена как дублирующая.
          `_require_registered_user(cursor, telegram_id)` тоже переведена
          на `get_worker()` — параметр `cursor` сохранён в сигнатуре
          нетронутым (14 вызывающих мест в подчастях 3.2-3.5 ещё не
          переведены и передают его по старой сигнатуре); уберётся одним
          заходом после подчасти 3.5. Везде на сетевую ошибку — не
          исключение наружу, а понятное сообщение рабочему в чат.
        - Попутно закрыт реальный баг, из-за которого бот падал бы уже
          при импорте модуля: в headless `backend/legacy/config.py` не
          было ни `DOLZHNOSTI`, ни остальных констант должностей
          (`DOLZHNOST_LESOVOD` и т.д.) — `DOLZHNOST_LABEL_TO_KEY` в
          `telegram_bot.py` строится на уровне модуля и обращался бы к
          несуществующим атрибутам. Перенесены из десктопного `config.py`
          без изменений. Заодно добавлена `get_api_base_url()`
          (`LESOVOD_API_BASE_URL`, по умолчанию `http://localhost:8000`).
        - `requirements.txt`: добавлены `requests` (HTTP-клиент бота) и
          `pyTelegramBotAPI` (сам `telebot` — раньше не был перечислен
          вообще, хотя `telegram_bot.py` его импортирует).
        - **Проверено:** через `TestClient` (полный lifespan-запуск,
          реальная запись в SQLite) — `GET` для незарегистрированного →
          404; без токена / с неверным токеном → 401; `PUT` (вставка,
          затем повторный upsert-апдейт) → 200 с ожидаемыми данными;
          `admin` тоже имеет доступ к `bot.access`. Отдельно проверен
          прямой импорт `telegram_bot.py` (раньше падал на
          `DOLZHNOST_LABEL_TO_KEY`, теперь нет) и построение меню.
          Сквозной прогон через реально поднятый `uvicorn` по сети
          (а не `TestClient`) не завершён — в среде, где готовился патч,
          фоновый процесс сервера не переживает между вызовами
          инструментов; для реального сервера это ограничение не
          актуально.
   3.2. ✅ **Отчёты о работе** — сделано. `save_raw_report`,
        `process_report_kvartal_step`, `process_report_vydel_step`,
        `process_report_photo_step`, `process_other_work_photo_step`,
        `process_report_confirm_duplicate_step`,
        `find_recent_duplicate_report`, `download_photo`. Основной
        сценарий "кнопка → квартал → выдел → фото" и защита от дублей.
        - Новые эндпоинты в `app/routers/bot.py` (тот же роутер, что и
          3.1, за тем же `require_permission("bot.access")`):
          `POST /api/bot/reports` (создание отчёта) и
          `GET /api/bot/reports/duplicate` (проверка на вероятный дубль
          перед сохранением).
        - SQL-логика не переписывалась, а перенесена: `save_raw_report`
          раньше жила в `telegram_bot.py`, принимала `cursor` и не
          коммитила сама (коммит был на совести вызывающего кода,
          открывавшего общее sqlite-соединение) — теперь это функция в
          `legacy/db.py` (`save_raw_report(conn, ...)`), коммитит сама,
          т.к. вызывается из отдельного HTTP-запроса без общего
          вызывающего кода. `find_recent_duplicate_report` в `db.py` не
          менялась вообще — просто теперь вызывается из
          `app/routers/bot.py`, а не напрямую из `telegram_bot.py`.
        - `POST /api/bot/reports` не принимает ФИО в теле запроса —
          берёт его из `lesorub_directory` по `telegram_id` (тот же
          профиль, что и `GET /api/bot/workers/{telegram_id}`), чтобы не
          полагаться на значение, которое бот мог держать в памяти
          диалога с более раннего шага опроса.
        - Побочно найденный и исправленный баг: `Depends(get_conn)` в
          `app/database.py` не выставляет `conn.row_factory =
          sqlite3.Row` (в отличие от `get_db()` в `telegram_bot.py`,
          которым раньше пользовались эти функции) — а
          `find_recent_duplicate_report` обращается к результату по
          имени колонки (`row["data_soobscheniya"]` и т.п.). Менять
          `row_factory` глобально для всех роутеров рискованно (сломало
          бы позиционную распаковку кортежей в других местах, например
          в `_row_to_worker_dict` из части 3.1) — выставляется локально,
          только на соединении внутри `GET /api/bot/reports/duplicate`.
        - `telegram_bot.py`: добавлены HTTP-обёртки
          `check_duplicate_report()`/`create_raw_report()` (по образцу
          `get_worker()`/`save_registration()` из 3.1 — на сетевую
          ошибку не исключение наружу, а понятный текст рабочему в чат).
          Старые `save_raw_report(cursor, ...)` и импорт
          `find_recent_duplicate_report` из `db` удалены как дублирующие
          (тот же принцип, что в 3.1 с `_save_registration`).
        - `process_report_kvartal_step`, `process_report_photo_step`,
          `process_other_work_photo_step`,
          `process_report_confirm_duplicate_step` — переведены на новые
          HTTP-обёртки; заодно перестали открывать локальное
          sqlite-соединение (`get_db()`), которое в них теперь было бы
          не для чего использовать — `_require_registered_user` с части
          3.1 уже не трогает переданный `cursor` (см. её докстринг),
          так что вызывающему коду, у которого нет других причин иметь
          соединение, незачем его открывать. `process_report_vydel_step`
          не тронута — она и раньше не обращалась к БД вообще (только
          парсит номера выделов).
        - `_require_registered_user(cursor, telegram_id)` — сигнатура
          по-прежнему не менялась (как и предупреждала часть 3.1): в эту
          подчасть переведённые вызовы передают `None` вместо `cursor`
          (значение всё равно не используется); места из подчастей
          3.3-3.5, ещё не переведённые, продолжают передавать реальный
          `cursor` по старой сигнатуре. Параметр уберётся одним заходом
          после подчасти 3.5, как и было решено в 3.1.
        - `download_photo` — НЕ изменена и не переводится на HTTP: это
          вызовы Telegram Bot API (`bot.get_file`/`bot.download_file`) и
          локальная запись файла на диск бота, а не обращение к нашему
          backend'у. В `POST /api/bot/reports` передаётся уже готовый
          `photo_path` — тот же принцип, что `app/routers/ai_log.py`
          (`GET /photo`) уже использует для `completed_works.photo_path`
          (отдаёт файл по абсолютному пути, только если он числится в
          `raw_reports`/`completed_works`).
        - **Проверено:** через `TestClient` (полный lifespan-запуск,
          реальная запись в SQLite, `geopandas`/`shapely`/`folium`
          установлены в среде патча и подтверждённо импортируются) —
          регистрация рабочего → `POST /api/bot/reports` создаёт запись
          в `raw_reports` (видна через уже существующий
          `GET /api/ai-log/raw-reports`, без изменений в нём) с верным
          `ispolnitel_fio`, взятым из `lesorub_directory`, а не из тела
          запроса; `GET /api/bot/reports/duplicate` — до создания отчёта
          возвращает `{"duplicate": null}`, после — находит его при тех
          же кв/выд/типе работы, не находит при других выделах (сравнение
          как множества, порядок не важен — та же логика
          `find_recent_duplicate_report`); отчёт "Другая работа"
          (`kvartal`/`vydels` не переданы) создаётся так же; отчёт от
          незарегистрированного `telegram_id` → 404; без токена → 401.
          Отдельно проверен прямой импорт `telegram_bot.py` (падений на
          `NameError`/отсутствующих символах нет, `save_raw_report`/
          `find_recent_duplicate_report` в модуле больше не определены —
          удалены осознанно, не забыты) и то, что `ast.parse` проходит
          на всех изменённых файлах. Сквозной прогон через реально
          поднятый `uvicorn` по сети (а не `TestClient`) — то же
          ограничение среды, что было отмечено в 3.1 (фоновый процесс
          сервера не переживает между вызовами инструментов); для
          реального сервера оно не актуально.
   3.3. ✅ **Поломки и служебные записки** — сделано.
        `process_breakdown_step`, `handle_breakdown_add_callback`,
        `process_zametki_list`, `handle_zametka_done_callback`,
        `save_breakdown_report`, `add_sluzhebnaya_zametka_from_breakdown`,
        `list_active_sluzhebnye_zametki`, `complete_sluzhebnaya_zametka`,
        `get_lesnichiy_telegram_ids`, `_format_zametka_button_label`.
        - Новые эндпоинты в `app/routers/bot.py` (тот же роутер, что и
          3.1/3.2, за тем же `require_permission("bot.access")`):
          `POST /api/bot/breakdowns` (отчёт о поломке — раньше два
          отдельных прямых обращения к `db.py`,
          `save_breakdown_report`+`get_lesnichiy_telegram_ids`, по общему
          sqlite-соединению бота, теперь один HTTP-запрос, возвращающий
          сразу id новой поломки и список telegram_id лесничих — боту
          нужны оба результата сразу, чтобы разослать уведомление),
          `POST /api/bot/breakdowns/{id}/notes` (лесничий переносит
          поломку в служебные записки), `GET /api/bot/zametki` (список
          активных записок) и `POST /api/bot/zametki/{id}/complete`
          (отметка выполненной).
        - В отличие от части 3.2, SQL-логика никуда не переезжала: все
          пять функций (`get_lesnichiy_telegram_ids`/
          `save_breakdown_report`/`add_sluzhebnaya_zametka_from_breakdown`/
          `list_active_sluzhebnye_zametki`/`complete_sluzhebnaya_zametka`)
          и раньше жили в `legacy/db.py`, а не в `telegram_bot.py` (в
          отличие от `save_raw_report` из 3.2) — роутер просто вызывает
          их вместо `telegram_bot.py`.
        - `POST /api/bot/breakdowns` не принимает ФИО/должность в теле
          запроса — берёт их из `lesorub_directory` по `telegram_id`, тот
          же принцип, что и `POST /api/bot/reports` из части 3.2 (не
          полагаться на значение, которое бот держит в памяти диалога).
        - `POST /api/bot/breakdowns/{id}/notes` возвращает
          `{"zametka_id": null}` вместо 404/409, если поломка не найдена
          или уже была обработана раньше (двойное нажатие кнопки
          несколькими лесничими одновременно) — тот же принцип "ожидаемый
          исход — не HTTP-ошибка", что уже применён в
          `GET /api/bot/reports/duplicate` из части 3.2 (там тоже
          `{"duplicate": null}`): боту не нужно различать "не найдена" и
          "уже добавлена", в обоих случаях он просто сообщает лесничему,
          что кнопка уже недействительна.
        - Тот же нюанс с `row_factory`, что и в части 3.2
          (`GET /api/bot/reports/duplicate`): `add_sluzhebnaya_zametka_
          from_breakdown` и `list_active_sluzhebnye_zametki` обращаются к
          результату по имени колонки, `Depends(get_conn)` не выставляет
          `row_factory=sqlite3.Row` глобально — выставляется локально, на
          соединении внутри каждого из двух эндпоинтов, которым это
          нужно; `complete_sluzhebnaya_zametka` в этом не нуждается
          (возвращает `bool` по `cursor.rowcount`).
        - `telegram_bot.py`: добавлены HTTP-обёртки
          `create_breakdown_report()`/`add_breakdown_to_notes()`/
          `get_active_zametki()`/`complete_zametka()` (по образцу
          обёрток из 3.1/3.2 — на сетевую ошибку не исключение наружу, а
          понятный текст рабочему/лесничему в чат; для `answer_callback_
          query` текст короче, т.к. Telegram ограничивает ответ на
          callback примерно 200 символами). Старые прямые импорты
          `get_lesnichiy_telegram_ids`/`save_breakdown_report`/
          `add_sluzhebnaya_zametka_from_breakdown`/
          `list_active_sluzhebnye_zametki`/`complete_sluzhebnaya_zametka`
          из `db` удалены из `from db import (...)` как больше не
          используемые напрямую (тот же принцип, что в 3.1/3.2 с
          `_save_registration`/`save_raw_report`).
        - `process_breakdown_step`, `handle_breakdown_add_callback`,
          `process_zametki_list`, `handle_zametka_done_callback` —
          переведены на новые HTTP-обёртки; `get_db()` внутри них больше
          не открывается. `_require_registered_user` в
          `process_breakdown_step` теперь вызывается с `cursor=None` (тот
          же переходный приём, что в 3.2 — параметр `cursor` в сигнатуре
          `_require_registered_user` не используется, но пока не убран:
          вызовы из ещё не переведённых подчастей 3.4-3.5 продолжают
          передавать реальный `cursor`; уберётся одним заходом после
          подчасти 3.5, как решено в 3.1). `get_db()` сам по себе не
          удалён — им по-прежнему пользуются ещё не переведённые
          обработчики подчастей 3.4/3.5.
        - `_format_zametka_button_label` — не менялась: чистая функция
          форматирования, к БД не обращается.
        - **Проверено:** через `TestClient` (полный lifespan-запуск,
          реальная запись в SQLite, `geopandas`/`shapely`/`folium`/
          `requests`/`pyTelegramBotAPI` установлены в среде патча и
          подтверждённо импортируются) — регистрация рабочего
          (тракторист) и лесничего → `POST /api/bot/breakdowns` создаёт
          запись с верными `fio`/`dolzhnost`, взятыми из
          `lesorub_directory`, и возвращает telegram_id лесничего в
          `lesnichiy_telegram_ids`; от незарегистрированного telegram_id
          → 404; без токена → 401; `POST /api/bot/breakdowns/{id}/notes`
          создаёт заметку (видна через `GET /api/bot/zametki` с верными
          `source='поломка'`/текстом/статусом `активна`), повторный вызов
          на ту же поломку и вызов на несуществующий id — оба возвращают
          `{"zametka_id": null}`; `POST /api/bot/zametki/{id}/complete` →
          `{"done": true}`, повторный вызов и вызов на несуществующий id →
          `{"done": false}`; после завершения заметка пропадает из
          `GET /api/bot/zametki`. Отдельно проверен прямой импорт
          `telegram_bot.py` (падений на отсутствующих символах нет,
          `save_breakdown_report`/`get_lesnichiy_telegram_ids`/
          `add_sluzhebnaya_zametka_from_breakdown`/
          `list_active_sluzhebnye_zametki`/`complete_sluzhebnaya_zametka`
          в модуле больше не определены/не импортированы — убраны
          осознанно; `get_db` по-прежнему на месте) и построение меню.
          `ast.parse` проходит на обоих изменённых файлах. Сквозной
          прогон через реально поднятый `uvicorn` по сети — то же
          ограничение среды, что и в 3.1/3.2 (фоновый процесс сервера не
          переживает между вызовами инструментов); для реального сервера
          оно не актуально.
   3.4. ✅ **Остатки, задачи, таксация, отмена отчёта** — сделано.
        - `get_active_delyanki_for_bot`/`get_tasks_for_bot`/
          `complete_task_for_bot`/`get_last_raw_report_for_user`/
          `cancel_raw_report` — SQL-логика осталась в `db.py` как есть
          (не переезжала, тот же приём, что и в 3.2/3.3); в
          `app/routers/bot.py` добавлены пять новых эндпоинтов:
          `GET /api/bot/delyanki?kvartal=`, `GET /api/bot/tasks
          ?telegram_id=`, `POST /api/bot/tasks/{id}/complete
          ?telegram_id=`, `GET /api/bot/raw-reports/last?telegram_id=`,
          `DELETE /api/bot/raw-reports/{id}?telegram_id=` — каждый под
          `require_permission("bot.access")`, с локальным
          `conn.row_factory = sqlite3.Row` там, где вызываемая функция
          читает столбцы по имени (тот же нюанс, что в 3.2/3.3).
          `telegram_bot.py`: `process_cancel_last_report(_confirm_step)`,
          `process_balance_step`, `handle_taskdone_callback` и блок
          `BTN_TASKS` в `handle_message` переведены на новые HTTP-обёртки
          (`get_last_report`/`cancel_report`/`get_active_delyanki`/
          `complete_task`/`get_worker_tasks`) и больше не открывают
          `get_db()` — регистрация проверяется через
          `_require_registered_user(None, telegram_id)`, как и в 3.2/3.3.
        - `get_vydel_card` — **прямой своп (находка "низкий риск"), а не
          новый эндпоинт**: тот же SQL уже был доступен вебу через
          `GET /api/taxation/vydel` (`app/routers/taxation.py`, без
          требования `bot.access` — эндпоинт и так открыт на чтение).
          `process_taxation_step` теперь ходит именно туда через новую
          обёртку `get_taxation_card()` — дублирующий эндпоинт в
          `app/routers/bot.py` заводить не потребовалось.
        - **Находка №1 аудита (ЕГАИС-сверка) — решение принято.**
          `get_remaining_volumes_for_bot` в `legacy/raskhod.py` (которым
          пользуется бот) и в `raskhod_v2.py` (которым пользуется веб)
          расходятся: `raskhod_v2.py` НЕ делает сверку с ЕГАИС.
          Переведи бот на HTTP как есть (`GET /api/raskhod/remaining`,
          через `raskhod_v2.py`) — он тихо потерял бы защиту от переруба.
          Выбран вариант **"б"**: `handle_balance_callback` **сознательно
          НЕ переведена** — по-прежнему вызывает
          `raskhod.get_remaining_volumes_for_bot(conn, ...)` напрямую,
          локальным sqlite-соединением (`get_db()`, единственное
          оставшееся прямое обращение бота к БД после этой подчасти).
          Вариант "а" (перенести ЕГАИС-сверку в `raskhod_v2.py`, чтобы
          веб тоже её получил) не выбран — это отдельная, более
          рискованная правка живой логики баланса для веб-интерфейса
          лесничих, не входящая в объём "перевести бота на HTTP"; если
          понадобится дать вебу ту же сверку — отдельный заход.
          Подробное обоснование — в комментарии у импорта
          `from raskhod import get_remaining_volumes_for_bot` в начале
          `telegram_bot.py` и в докстринге `handle_balance_callback`.
        - `_build_balance_callback_data`/`_format_remaining_reply`/
          `_format_taxation_reply`/`_format_tasks_reply` — не менялись:
          чистые функции форматирования, к БД не обращаются напрямую
          (только к уже готовым dict/строкам).
        - **Проверено:** через `TestClient` (полный lifespan-запуск,
          реальная запись в SQLite) — все пять новых эндпоинтов: чужой
          `telegram_id` не может завершить/отменить чужую
          задачу/отчёт (`{"done": false}`/`{"cancelled": false}`),
          повторный вызов на уже закрытую задачу/отменённый отчёт — тот
          же результат; список делянок фильтрует уже завершённые
          (`status_rabot='выполнено'`); без токена → 401. Отдельно
          проверен прямой импорт `telegram_bot.py` (падений на
          отсутствующих символах нет, все шесть перечисленных выше
          функций из `db.py` в модуле больше не определены/не
          импортированы — убраны осознанно; `get_db` и
          `get_remaining_volumes_for_bot` по-прежнему на месте, как и
          решено). `ast.parse` проходит на обоих изменённых файлах.
        - **Сквозной прогон через реально поднятый `uvicorn`, в отличие
          от 3.1-3.3, в этот раз получилось сделать** (сервер и вызовы
          выполнены в рамках одной команды одним заходом, а не в
          отдельных вызовах инструментов) — реальные HTTP-обёртки
          `telegram_bot.py` вызваны по-настоящему, по сети, против
          настоящего `uvicorn`, с теми же результатами, что и в
          `TestClient`.
        - **Новая находка (не входила в аудит, обнаружена при этом
          сквозном прогоне) — гонка потоков в `app/database.py`,
          затрагивает ВСЕ роутеры, не только бота.** Под конкурентной
          нагрузкой (проверено на `GET /api/bot/raw-reports/last` и,
          для контроля, на уже существующем с части 3.1
          `GET /api/bot/workers/{id}` — воспроизводится одинаково на
          обоих, значит дело не в новом коде части 3.4, а в
          `get_conn()` как таковой) до ~75% запросов падают с 500:
          `sqlite3.ProgrammingError: SQLite objects created in a thread
          can only be used in that same thread` — при закрытии
          соединения в `finally` блоке `get_conn()`. Похоже на известную
          особенность FastAPI/anyio: `Depends()`-генератор на обычной
          (не `async def`) функции выполняется в threadpool, но вход и
          выход из него не гарантированно попадают на один и тот же
          поток пула — а `sqlite3.connect()` по умолчанию привязывает
          соединение к потоку, где оно создано. Каждый запрос и так
          получает своё, не разделяемое между запросами соединение (см.
          docstring `get_connection()`), поэтому фактического
          одновременного доступа к одному соединению из двух потоков
          нет — только его открытие и закрытие иногда происходят на
          разных потоках пула. Проверенный локально однострочный фикс:
          `sqlite3.connect(DB_PATH, check_same_thread=False)` в
          `get_connection()` (`app/database.py`) — под той же
          конкурентной нагрузкой (40 параллельных запросов) убирает все
          500-е (40/40 успешных против 30/40 неуспешных без фикса).
          **В этот патч фикс НЕ включён** — `app/database.py`
          используется вообще всеми роутерами (не только `bot.py`),
          это соседняя система, а не то, что затронуто переводом бота
          на HTTP; правка отдельного, общего для всего backend'а файла
          заслуживает отдельного захода и отдельного тестирования, а не
          довеском к части 3.4. Важно для реального запуска: в проде
          бот работает через polling (`bot.polling(...)`,
          pyTelegramBotAPI по умолчанию однопоточный на приём апдейтов,
          но при `threaded=True`, включённом по умолчанию, обработчики
          на разные апдейты выполняются в разных потоках) — если
          несколько рабочих одновременно напишут боту (реалистичный
          сценарий, например, утром при массовой отправке отчётов),
          конкурентные HTTP-запросы от бота к backend'у попадут именно
          в эту гонку. Настоятельно рекомендуется применить
          однострочный фикс выше отдельным, самостоятельным патчем до
          прода — независимо от готовности частей 3.5/Блока 4.
   3.5. ✅ **Маршрутизация сообщений, геометки, запуск** — сделано, этим
        заходом. `handle_location`, `_save_geo_note`, `handle_message`,
        `handle_photo`, `run_bot`, вызов `migrate_schema` при старте.
        - **Гео-заметки** — новая HTTP-обёртка `save_geo_note()` в
          `telegram_bot.py` → `POST /api/bot/geo-notes` (новый эндпоинт в
          `app/routers/bot.py`, за тем же `require_permission
          ("bot.access")`). Раньше `_save_geo_note(cursor, ...)` писала в
          таблицу `geo_notes` прямо в `telegram_bot.py`, без своего
          commit — коммитил вызывающий код (`handle_message`/
          `handle_photo`). В отличие от подчастей 3.2-3.4, INSERT сделан
          прямо в роутере (по образцу `upsert_worker()` из 3.1), а не как
          новая функция в `legacy/db.py` — таблица однострочная, без
          update/upsert-логики, заводить под неё функцию в `db.py`
          показалось лишним усложнением ради одного `INSERT`.
          `_save_geo_note(cursor, ...)` удалена как дублирующая (тот же
          принцип, что в 3.1-3.2 с `_save_registration`/`save_raw_report`).
        - `handle_message`: четыре места, где раньше открывался `get_db()`
          ради `cursor` для `_require_registered_user` (блоки
          `BTN_OTHER_WORK`, `REPORT_BUTTON_TIP`, `BTN_BREAKDOWN` и
          финальная ветка "ни одна кнопка не совпала"), переведены на
          `_require_registered_user(telegram_id)` без параметра `cursor`
          (см. ниже) — локальное sqlite-соединение в этих местах больше
          не открывается. Пятое место — привязка комментария к ожидающей
          гео-метке — переведено на `save_geo_note()`.
        - `handle_photo`: то же самое для фото-комментария к геометке —
          `save_geo_note()` вместо прямого `get_db()`+`cursor.execute`.
        - `handle_location` — рассмотрена, **оставлена без изменений**: и
          раньше не обращалась к БД вообще, только кладёт координаты в
          `_pending_geo` (память процесса бота, не таблица) — переводить
          было нечего, только добавлено пояснение в докстринг.
        - `run_bot()`/вызов `migrate_schema` при старте — рассмотрены и
          **сознательно оставлены без изменений**. Backend (`app/main.py`,
          startup-событие) и так уже вызывает `migrate_schema` сам при
          своём запуске — казалось бы, вызов в `run_bot()` дублирующий.
          Не убран по двум причинам: (1) `handle_balance_callback` (часть
          3.4, сознательное исключение) по-прежнему держит своё прямое
          sqlite-соединение к тому же `DB_PATH` — бот не может полагаться
          на то, что backend гарантированно стартовал первым и успел
          создать схему (порядок автозапуска служб Windows, Блок 4, не
          гарантирован сам по себе без явной настройки зависимостей
          служб); (2) `migrate_schema` идемпотентна (`CREATE TABLE IF NOT
          EXISTS` + добавление недостающих колонок) — повторный вызов
          подряд из двух процессов ничего не портит, цена на каждый
          старт бота (не на каждое сообщение) пренебрежимо мала. Если
          `get_remaining_volumes_for_bot` когда-нибудь переедет на HTTP
          (вариант "а" из решения 3.4, не выбранный там) — тогда у бота
          не останется ни одного прямого обращения к БД, и вызов можно
          будет убрать вместе с `DB_PATH`/`get_db()`/`import sqlite3`
          одним заходом; отдельная, не входящая в объём 3.5 правка.
        - `_require_registered_user(cursor, telegram_id)` — параметр
          `cursor` убран из сигнатуры (стал
          `_require_registered_user(telegram_id)`), как и обещала часть
          3.1: он не использовался функцией уже с 3.1 (проверка ходит по
          HTTP через `get_worker()`), подчасти 3.2-3.4 уже вызывали её как
          `_require_registered_user(None, telegram_id)`, а часть 3.5
          перевела последние 4 места, передававшие реальный `cursor`. Все
          15 вызовающих мест в файле обновлены одним заходом на новую
          однопараметрическую сигнатуру.
        - **Итог по всему Блоку 3, после этой подчасти**: единственное
          оставшееся прямое обращение бота к БД — `get_remaining_volumes_
          for_bot` в `handle_balance_callback` (сознательное исключение
          части 3.4, находка №1 аудита, ЕГАИС-сверка) + сама возможность
          его сделать (`get_db()`/`DB_PATH`/`import sqlite3`, нужны и для
          `migrate_schema` в `run_bot()`, решение выше). Всё остальное —
          14 исходных точек импорта из `db.py`/`raskhod.py` и ~33 места
          прямой работы с sqlite внутри хендлеров (аудит, пункт 1) —
          переведено на HTTP через `app/routers/bot.py` (плюс прямой своп
          таксации на уже существующий `app/routers/taxation.py`).
        - **Проверено:** прямой импорт `telegram_bot.py` (падений на
          отсутствующих символах нет; `_save_geo_note` в модуле больше не
          определена — удалена осознанно, не забыта; `save_geo_note`,
          `get_db`, `run_bot`, `handle_location` на месте, как и решено;
          `_require_registered_user` — новая однопараметрическая
          сигнатура). `ast.parse` проходит на обоих изменённых файлах
          (`telegram_bot.py`, `app/routers/bot.py`). Сквозной прогон
          через реально поднятый `uvicorn` (не только прямой импорт) —
          `save_registration()` → `save_geo_note()` по-настоящему, по
          сети, против настоящего сервера: запись реально появляется в
          таблице `geo_notes` (проверено прямым SQL-запросом к файлу
          базы) с верными `telegram_id`/`lat`/`lon`/`note_text`; запрос
          без токена → 401, как и на всех остальных эндпоинтах роутера.
4. ✅ **Проверка и обновление плана** — сделано, этим же заходом
   (см. пункт 3.5 выше и итоговую отметку статуса блока).

Исходная постановка задачи (для справки, без изменений):

Независимый блок, можно делать параллельно с чем угодно.

1. Пройтись по `telegram_bot.py`, найти все прямые импорты
   `from raskhod import ...`, `from db import ...`,
   `from delyanka import ...`.
2. Заменить на HTTP-вызовы к тому же backend (`requests`/`httpx` к
   `http://localhost:8000/api/...`) — бот и так уже "ещё один клиент",
   просто пока не через API.
3. **Важно — сначала решить дублирование**: бот сейчас использует
   старый `legacy/raskhod.py`, веб — `raskhod_v2.py`. Если переводить
   бота на API, он автоматически начнёт использовать
   `raskhod_v2.py`-логику (через роутер) — это хорошо (единая логика),
   но нужно явно сверить, что поведение `get_remaining_volumes_for_bot`
   не разошлось между двумя файлами настолько, чтобы сломать
   существующие сценарии бота.
4. Бот, скорее всего, не нуждается в токене авторизации (он на том же
   сервере) — либо завести ему отдельного служебного пользователя с
   ролью `admin`/новой ролью `bot`, если Блок 1 уже сделан и роутеры
   требуют токен.

**Объём:** зависит от того, сколько всего мест в боте бьёт напрямую в
БД — нужно сначала посчитать
(`grep -n "^from db\|^from raskhod\|^from delyanka" telegram_bot.py`).
По объёму работы — вероятно средний, но механический (замена вызова
функции на HTTP-запрос), не творческий.
**Зависимость:** если делать после Блока 1 — нужно продумать
авторизацию бота отдельно.

---

## Блок 4. Развёртывание — самый большой нетронутый кусок

> **Статус: сделано (Этап 3 доработки).** Ниже — исходная постановка
> задачи, а после каждого пункта отметкой ✅ — что именно внесено.
> Всё — новые файлы в отдельной папке `deploy/` (архив
> `lesovod_stage7_block4_deploy.zip`, распаковать поверх проекта, в
> корень, рядом с `backend/` и `frontend/`); ни один существующий файл
> приложения не менялся.

1. **Backend**: `uvicorn app.main:app --host 0.0.0.0 --port 8000` за
   **Caddy** (проще nginx — сам получает HTTPS, если понадобится) —
   Caddy отдаёт собранный `frontend/dist` статикой и проксирует
   `/api/*` на uvicorn.
   ✅ `deploy/Caddyfile` — два `handle`-блока: `/api/*` проксируется на
   `127.0.0.1:8000` (проверяется первым, взаимоисключающе со вторым
   блоком — так `/api/*` не долетает до статики), всё остальное — из
   `frontend/dist` через `file_server` с `try_files {path} /index.html`
   (без этого обновление страницы на клиентском роуте React Router,
   например `/raskhod`, давало бы 404 вместо приложения). Комментарием
   в файле — как включить автоматический HTTPS (реальный домен) или
   `tls internal` (самоподписанный, для чисто локальной сети).
2. **Автозапуск на Windows** — служба через `nssm`
   (`nssm install LesovodBackend ...`) с автоперезапуском при сбое.
   ✅ `deploy/install_service.bat` — `nssm install LesovodBackend`,
   `Start SERVICE_AUTO_START` + `AppExit Default Restart` (автозапуск и
   автоперезапуск, как в постановке), плюс попутно решённое: логи
   (`AppStdout`/`AppStderr` в `logs/`, с ротацией по размеру) — без
   этого у службы просто нет консоли и вывод uvicorn терялся бы
   целиком. Служба запускает не `python` напрямую, а
   `run_server.bat` (см. п.5) — так подхватывается `.env`.
3. **Локальный IP/имя сервера** — закрепить IP в роутере или настроить
   `lesovod.local` через mDNS (Bonjour на Windows), чтобы адрес не
   менялся при перезагрузке роутера.
   ✅ Задокументировано в `deploy/README.md` (раздел 4) как
   административные шаги, а не скрипт — закрепление IP через
   DHCP-резервацию в роутере (основной, надёжный вариант) плюс
   `lesovod.local` через Bonjour Print Services поверх него
   (удобный, но менее предсказуемый — честно отмечено, что mDNS-имя не
   на всех Windows-клиентах резолвится одинаково надёжно, и дан обход
   через `hosts`-файл на случай проблемного клиента).
4. **Бэкап** — `.bat`-скрипт: копия `lesovod.db` (или
   `legacy/lesovod.db`, если backend работает со своей копией) +
   `storage/documents/` в папку с датой в имени, плюс `secrets.json`
   (если он есть — но подумайте, точно ли его нужно бэкапить в
   общедоступное место, или это должен быть отдельный, более
   защищённый бэкап). Через Планировщик заданий Windows — ежедневно.
   ✅ `deploy/backup_lesovod.bat` — папка с датой/временем в имени
   внутри `backups/`; `storage/` копируется целиком. `secrets.json`
   — решение по второй половине пункта принято явно: бэкапится
   **отдельно**, в `backups-secrets/` (не в общий `backups/`), с
   комментарием ограничить NTFS-права на эту папку только
   администратору. Старше 14 дней — само чистится (`forfiles`).
   Есть источник, добавленный по месту, что не было в исходной
   формулировке: копия `lesovod.db` берётся не простым `copy`, а через
   `sqlite3.Connection.backup()` (вызовом `venv`-python'а backend'а) —
   в WAL-режиме (Блок 1/Этап 2, `PRAGMA journal_mode=WAL` в
   `app/database.py`) рядом с файлом базы есть `-wal`/`-shm` с ещё не
   влитыми изменениями, обычное копирование файла их не подхватит и
   может дать бэкап без части последних записей; есть fallback на
   простой `copy`, если `venv` не найден, с явным предупреждением в
   выводе скрипта, что такой бэкап может быть неполным.
5. **`.env`/переменные окружения** для продакшн-запуска:
   `LESOVOD_DB_PATH`, `LESOVOD_RESOURCE_DIR`, `LESOVOD_APP_DIR` —
   прописать в файле `.env` рядом с сервисом, а не в переменных
   окружения ОС (иначе трудно ревизировать при переустановке).
   ✅ `deploy/.env.example` — шаблон с `LESOVOD_APP_DIR`,
   `LESOVOD_RESOURCE_DIR`, `LESOVOD_DB_PATH` (все три — как в
   постановке) плюс `LESOVOD_STORAGE_DIR` (нужна `app/paths.py`,
   отдельная от `legacy/config.py` переменная — в исходном плане не
   упомянута, но без неё бэкап п.4 не знал бы, что копировать) и
   `LESOVOD_PORT`. Само чтение `.env` в переменные окружения процесса
   — не в коде приложения (`main.py` не менялся), а в новом
   `deploy/run_server.bat`, который парсит `.env` построчно и запускает
   `uvicorn` из `venv` — именно его вызывает служба nssm (п.2) и его же
   логику переиспользует бэкап-скрипт (п.4), чтобы узнать пути к
   данным.

**Сознательно не тронуто:** `CORSMiddleware(allow_origins=["*"])` в
`backend/app/main.py` — комментарий в самом файле и так предупреждал
сузить перед продакшеном; раз Caddy теперь отдаёт фронтенд и
проксирует API с одного адреса (см. `Caddyfile`), запросы идут
same-origin, и `*` здесь уже не даёт постороннему сайту прямого
доступа исключительно за счёт этой настройки. Явную правку списка
источников в этот блок не включали — при желании сузить это
отдельная, тривиальная правка одной строки в `main.py`.

**Проверено:** `.bat`-файлы и `Caddyfile` проверены только чтением
(синтаксис/логика вручную) — в среде, где готовился патч, нет Windows
и нет реального сервера, поэтому живого прогона `nssm install`,
`Caddy run` и `schtasks` не было. Перед первым боевым запуском стоит
пройтись по чек-листу в `deploy/README.md` (раздел 6) и один раз
руками проверить, что бэкап реально разворачивается обратно — это
явно вынесено в чек-лист отдельным пунктом.

**Объём:** большой, но не программирование — административная
настройка + пара скриптов. По времени сопоставимо с Этапом 4 (весь
фронтенд), только другого рода работа. — подтвердилось: сами файлы
небольшие (6 скриптов/конфигов + README), но каждый шаг требует
реального Windows-сервера для проверки, чего не было в этом заходе.
**Зависимость:** логично делать после того, как Блок 1 (авторизация)
закрыт — иначе разворачиваете сервер, доступный всем в сети без
логина. Чек-лист в `deploy/README.md` явно проверяет это первым
пунктом.

---

## Блок 5. Мелкие хвосты (не блокируют ничего, можно по одному между делом)

Из того, что сами же авторы предыдущих этапов честно пометили как
несделанное:

| Хвост | Где | Сложность | Статус |
|---|---|---|---|
| Кнопки "Проверить ключ"/"Проверить токен" (Gemini/Telegram) | `Settings.jsx` + нужен новый эндпоинт `POST /api/settings/secrets/{name}/test` | Маленькая | — |
| Путь к LibreOffice (`soffice.exe`) как настройка | `settings.py` — нужен `GET/POST /api/settings/libreoffice-path` | Маленькая | — |
| Создание/удаление пресетов (комиссия/акт освидетельствования) | Сейчас только применение готового; эндпоинты `POST`/`DELETE .../presets/...` уже есть в backend | Маленькая, чисто фронтенд | — |
| "Отправить"/"Архивировать" в панели массовых действий документов | Нужно сперва решить: куда отправлять (email? Telegram?), и что значит "архив" для документа (схема) | Требует решения, не только кода | — |
| `dashboard.py` и `screens/raskhod/balance.py` тянут `PySide6` в шапке (try/except спасает, но хрупко) | Вынести чистые функции (`compute_items_totals`, `_compute_fire_danger` и т.п.) в Qt-свободный модуль | Маленькая-средняя, но именно то, о чём с самого начала предупреждал Этап 0 аудита | — |
| Полноценный предпросмотр `.docx` в браузере | Отдельная опция через LibreOffice headless → PDF, план сразу называл это опциональным | Средняя, не приоритет | — |
| Абрис: `upload_abris` не проверяет существование `item_id` | Мелкий баг, унаследован от desktop | Тривиальная | ✅ сделано попутно с Блоком 1 |

---

## Рекомендуемый порядок

1. ~~**Блок 1** (авторизация) — сегодня-завтра, дёшево и закрывает
   главную дыру в безопасности.~~ ✅ сделано.
2. ~~**Блок 4** — Caddy/nssm/бэкап/`.env`.~~ ✅ сделано (файлы готовы;
   реальный прогон на боевом Windows-сервере — при первом
   развёртывании, см. чек-лист в `deploy/README.md`).
3. ~~**Блок 2** (карта) — следующий по важности содержательный
   кусок.~~ ✅ сделано.
4. **Блок 3** (бот) — когда будет время, независимо от 1-2-4.
   Остался следующим по очереди.
5. **Блок 5** — по одному, между делом, когда правите соседний файл.
